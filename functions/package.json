{
  "name": "newscollect-functions",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^13.6.1",
    "firebase-functions": "^6.0.0",
    "@google/generative-ai": "^0.24.1",
    "cheerio": "^1.2.0"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20"
  },
  "private": true
}
